{
  "name": "continuation-passing-style",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "run:echo": "node bin/echo.js",
    "tslint": "tslint --fix **/*.ts",
    "build": "tsc --outDir bin",
    "test": "jest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.16",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "tslib": "^1.10.0",
    "typescript": "^3.7.2"
  }
}
